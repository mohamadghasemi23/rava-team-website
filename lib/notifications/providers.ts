import 'server-only';
export type NotificationChannel='email'|'sms'|'whatsapp'|'push';export type NotificationMessage={outboxId:string;tenantId:string;channel:NotificationChannel;recipient:string;subject?:string|null;body:string;locale:string;idempotencyKey:string;metadata?:Record<string,unknown>};export type NotificationSendResult={ok:boolean;providerMessageId?:string;providerStatus?:string;retryable?:boolean;errorCode?:string};
export interface NotificationProvider{key:string;channel:NotificationChannel;send(message:NotificationMessage,config:{publicConfig:Record<string,unknown>;secretRef?:string|null}):Promise<NotificationSendResult>}
const providers=new Map<string,NotificationProvider>();export function registerNotificationProvider(p:NotificationProvider){if(!/^[a-z0-9_.-]{2,50}$/.test(p.key))throw new Error('invalid_notification_provider_key');providers.set(`${p.channel}:${p.key}`,p)}export function getNotificationProvider(channel:NotificationChannel,key:string){const p=providers.get(`${channel}:${key}`);if(!p)throw new Error('notification_provider_unavailable');return p}
// Provider adapters resolve secretRef only inside protected server infrastructure. Raw API keys/tokens must never be persisted in public_config, browser payloads, templates, logs or outbox metadata.
