'use client'
import {usePathname} from 'next/navigation'
export default function TenantStatusGate({status,name,children}:{status:string;name:string;children:React.ReactNode}){const path=usePathname();if(path.startsWith('/admin')||path.startsWith('/login')||path.startsWith('/auth'))return <>{children}</>;if(status==='suspended')return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:24,textAlign:'center'}}><div><span>RAVA PLATFORM</span><h1>این وب‌سایت موقتاً غیرفعال است</h1><p>سرویس عمومی «{name}» در حال حاضر در وضعیت تعلیق قرار دارد.</p></div></main>;if(status==='maintenance')return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:24,textAlign:'center'}}><div><span>MAINTENANCE</span><h1>در حال بروزرسانی هستیم</h1><p>«{name}» به‌زودی دوباره در دسترس خواهد بود.</p></div></main>;if(status==='archived')return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:24,textAlign:'center'}}><div><h1>این سایت در دسترس نیست</h1></div></main>;return <>{children}</>}
