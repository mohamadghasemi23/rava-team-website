'use server';import{revalidatePath}from'next/cache';import{getTenantContext}from'@/lib/platform/tenant-context';export async function requestUpgrade(f:FormData){const a=await getTenantContext();if(a.platformWide||!['super_admin','admin'].includes(a.profile.role))throw new Error('forbidden');const feature=String(f.get('feature_key')||'').trim(),note=String(f.get('note')||'').trim().slice(0,500);if(!/^[a-z0-9._-]{3,80}$/.test(feature))throw new Error('invalid_feature');const{error}=await a.s.rpc('request_feature_upgrade',{p_tenant:a.tenantId,p_feature:feature,p_note:note||null});if(error)throw error;revalidatePath('/admin/upgrades')}
