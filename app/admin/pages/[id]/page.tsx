import Link from 'next/link'
import { notFound, redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import ActionForm from '../../components/ActionForm'
import { createBlock, deleteBlock, deletePage, moveBlock, toggleBlock, updateBlock, updatePage } from '../actions'

export const dynamic = 'force-dynamic'
const names:Record<string,string>={hero:'Hero',text:'متن',image:'تصویر',cta:'CTA',gallery:'گالری'}

export default async function PageEditor({params}:{params:Promise<{id:string}>}){
 const{id}=await params;const supabase=await createClient();const{data:claimsData}=await supabase.auth.getClaims();const userId=claimsData?.claims?.sub;if(!userId)redirect('/login');const{data:profile}=await supabase.from('profiles').select('role,active').eq('id',userId).single();if(!profile?.active)redirect('/login');
 const{data:page}=await supabase.from('pages').select('id,title,slug,status,seo').eq('id',id).single();if(!page)notFound();const seo=(page.seo??{})as{title?:string;description?:string};
 const{data:blocks}=await supabase.from('page_blocks').select('id,block_type,position,visible,data').eq('page_id',id).order('position',{ascending:true});
 return <main className="admin-shell">
  <header className="admin-head"><div><span>RAVA CONTROL CENTER</span><h1>ویرایش صفحه</h1></div><div className="admin-actions"><Link className="admin-link" href="/admin/pages">بازگشت به صفحات</Link></div></header>
  <section className="admin-panel"><ActionForm action={updatePage} className="admin-form" confirmTitle="ذخیره تغییرات صفحه" confirmMessage={`تغییرات صفحه «${page.title}» روی سرور ذخیره شود؟`} confirmLabel="بله، ذخیره شود"><input type="hidden" name="id" value={page.id}/><div className="admin-grid-2"><label>عنوان صفحه<input name="title" defaultValue={page.title} required/></label><label>آدرس صفحه<input name="slug" defaultValue={page.slug} required dir="ltr"/></label></div><label>وضعیت<select name="status" defaultValue={page.status}><option value="draft">پیش‌نویس</option><option value="published">منتشرشده</option><option value="hidden">مخفی</option><option value="scheduled">زمان‌بندی‌شده</option></select></label><div className="admin-grid-2"><label>عنوان SEO<input name="seo_title" defaultValue={seo.title??''}/></label><label>توضیح SEO<textarea name="seo_description" defaultValue={seo.description??''} rows={4}/></label></div><div className="admin-save-row"><button type="submit">ذخیره تغییرات</button>{page.status==='published'?<a className="admin-link" href={`/${page.slug}`} target="_blank">مشاهده صفحه</a>:null}</div></ActionForm></section>

  <section className="admin-panel"><h2>Block Builder</h2><p>سکشن‌های صفحه را از اینجا بساز، ویرایش کن، مرتب کن یا مخفی کن.</p><ActionForm action={createBlock} className="admin-form" confirmTitle="افزودن سکشن" confirmMessage="این سکشن به انتهای صفحه اضافه شود؟" confirmLabel="بله، اضافه شود"><input type="hidden" name="page_id" value={page.id}/><div className="admin-grid-2"><label>نوع سکشن<select name="block_type"><option value="hero">Hero</option><option value="text">متن</option><option value="image">تصویر</option><option value="cta">CTA</option><option value="gallery">گالری</option></select></label><div className="admin-save-row"><button type="submit">+ افزودن سکشن</button></div></div></ActionForm></section>

  {(blocks??[]).map((block,index)=>{const d=(block.data??{})as Record<string,unknown>;return <section className="admin-panel" key={block.id}><div className="admin-head"><div><span>BLOCK {index+1}</span><h2>{names[block.block_type]??block.block_type} {!block.visible?'— مخفی':''}</h2></div></div>
   <ActionForm action={updateBlock} className="admin-form" confirmTitle="ویرایش سکشن" confirmMessage={`تغییرات سکشن ${names[block.block_type]??block.block_type} ذخیره شود؟`} confirmLabel="بله، ذخیره شود"><input type="hidden" name="id" value={block.id}/><input type="hidden" name="page_id" value={page.id}/><input type="hidden" name="block_type" value={block.block_type}/>
    {(block.block_type==='hero'||block.block_type==='text'||block.block_type==='cta')&&<><label>عنوان<input name="title" defaultValue={String(d.title??'')}/></label><label>متن<textarea name="text" rows={5} defaultValue={String(d.text??'')}/></label></>}
    {(block.block_type==='hero'||block.block_type==='cta')&&<div className="admin-grid-2"><label>متن دکمه<input name="button_label" defaultValue={String(d.button_label??'')}/></label><label>لینک دکمه<input name="button_url" dir="ltr" defaultValue={String(d.button_url??'')}/></label></div>}
    {block.block_type==='image'&&<><label>آدرس تصویر<input name="url" dir="ltr" defaultValue={String(d.url??'')}/></label><div className="admin-grid-2"><label>Alt تصویر<input name="alt" defaultValue={String(d.alt??'')}/></label><label>کپشن<input name="caption" defaultValue={String(d.caption??'')}/></label></div></>}
    {block.block_type==='gallery'&&<><label>عنوان گالری<input name="title" defaultValue={String(d.title??'')}/></label><label>آدرس تصاویر — هر خط یک تصویر<textarea name="images" dir="ltr" rows={6} defaultValue={Array.isArray(d.images)?d.images.join('\n'):''}/></label></>}
    <button type="submit">ذخیره این سکشن</button></ActionForm>
    <div className="admin-grid-2" style={{marginTop:'1rem'}}><ActionForm action={toggleBlock} confirmTitle={block.visible?'مخفی کردن سکشن':'نمایش سکشن'} confirmMessage="وضعیت نمایش این سکشن تغییر کند؟"><input type="hidden" name="id" value={block.id}/><input type="hidden" name="page_id" value={page.id}/><input type="hidden" name="visible" value={String(block.visible)}/><button type="submit" className="admin-muted-button">{block.visible?'مخفی کردن':'نمایش دادن'}</button></ActionForm><div className="admin-actions"><ActionForm action={moveBlock} confirmTitle="جابه‌جایی سکشن" confirmMessage="این سکشن یک ردیف بالاتر برود؟"><input type="hidden" name="id" value={block.id}/><input type="hidden" name="page_id" value={page.id}/><input type="hidden" name="direction" value="up"/><button type="submit" className="admin-muted-button">↑ بالا</button></ActionForm><ActionForm action={moveBlock} confirmTitle="جابه‌جایی سکشن" confirmMessage="این سکشن یک ردیف پایین‌تر برود؟"><input type="hidden" name="id" value={block.id}/><input type="hidden" name="page_id" value={page.id}/><input type="hidden" name="direction" value="down"/><button type="submit" className="admin-muted-button">↓ پایین</button></ActionForm></div></div>
    <ActionForm action={deleteBlock} danger confirmTitle="حذف سکشن" confirmMessage="این سکشن و محتوای آن برای همیشه حذف شود؟" confirmLabel="بله، حذف شود"><input type="hidden" name="id" value={block.id}/><input type="hidden" name="page_id" value={page.id}/><button className="admin-danger-button" type="submit">حذف سکشن</button></ActionForm>
   </section>})}

  <section className="admin-panel admin-danger-zone"><h2>حذف صفحه</h2><p>این عملیات صفحه و Blockهای وابسته به آن را حذف می‌کند و قابل بازگشت نیست.</p><ActionForm action={deletePage} danger confirmTitle="حذف کامل صفحه" confirmMessage={`صفحه «${page.title}» و تمام محتوای وابسته به آن برای همیشه حذف شود؟`} confirmLabel="بله، برای همیشه حذف شود"><input type="hidden" name="id" value={page.id}/><button className="admin-danger-button" type="submit">حذف کامل صفحه</button></ActionForm></section>
 </main>
}
