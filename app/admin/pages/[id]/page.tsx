import Link from'next/link';import{notFound,redirect}from'next/navigation';import ActionForm from'../../components/ActionForm';import AutosaveWatcher from'../../components/AutosaveWatcher';import PageExperiencePanel from'./PageExperiencePanel';import SectionBuilder from'./SectionBuilder';import SectionOrderPanel from'./SectionOrderPanel';import{getTenantContext}from'@/lib/platform/tenant-context';import{canEditContent}from'@/lib/platform/capabilities';import{deletePage,updatePage}from'../actions';export const dynamic='force-dynamic'
export default async function PageEditor({params}:{params:Promise<{id:string}>}){const{id}=await params,a=await getTenantContext();if(!canEditContent(a))redirect('/admin');const{data:page}=await a.s.from('pages').select('id,title,slug,status,seo,page_type').eq('id',id).eq('tenant_id',a.tenantId).single();if(!page)notFound();const seo=(page.seo??{})as{title?:string;description?:string};return <main className="admin-shell" id="page-editor-root"><AutosaveWatcher tenantId={a.tenantId} entityType="page" entityId={page.id} rootId="page-editor-root" userId={a.userId}/><header className="admin-head"><div><span>RAVA CONTROL CENTER · {a.tenant.name}</span><h1>ویرایش صفحه</h1><p>اطلاعات پایه صفحه، Experience و Sectionهای نسخه‌دار از همین‌جا مدیریت می‌شوند.</p></div><div className="admin-actions"><Link className="admin-link" href={`/preview/page/${page.id}`} target="_blank">Preview Draft ↗</Link><Link className="admin-link" href={`/admin/pages/${page.id}/history`}>تاریخچه نسخه‌ها</Link><Link className="admin-link" href="/admin/pages">بازگشت</Link></div></header><section className="admin-panel"><ActionForm action={updatePage} className="admin-form" confirmTitle="ذخیره تغییرات صفحه" confirmMessage={`تغییرات صفحه «${page.title}» در همین سایت ذخیره شود؟`}><input type="hidden" name="id" value={page.id}/><div className="admin-grid-2"><label>عنوان صفحه<input name="title" defaultValue={page.title} required/></label><label>آدرس صفحه<input name="slug" defaultValue={page.slug} required dir="ltr"/></label></div><label>وضعیت<select name="status" defaultValue={page.status}><option value="draft">پیش‌نویس</option><option value="published">منتشرشده</option><option value="hidden">مخفی</option><option value="scheduled">زمان‌بندی‌شده</option></select></label><div className="admin-grid-2"><label>عنوان SEO<input name="seo_title" defaultValue={seo.title??''}/></label><label>توضیح SEO<textarea name="seo_description" defaultValue={seo.description??''} rows={4}/></label></div><div className="admin-save-row"><button type="submit">ذخیره اطلاعات صفحه</button>{page.status==='published'?<a className="admin-link" href={`/${page.slug}`} target="_blank">مشاهده نسخه عمومی</a>:null}</div></ActionForm></section><PageExperiencePanel pageId={page.id}/><SectionOrderPanel pageId={page.id}/><SectionBuilder pageId={page.id} pageType={page.page_type||'standard'}/><section className="admin-panel admin-danger-zone"><h2>حذف صفحه</h2><p>این عملیات فقط همین صفحه در Tenant فعال را حذف می‌کند.</p><ActionForm action={deletePage} danger confirmTitle="حذف کامل صفحه" confirmMessage={`صفحه «${page.title}» و محتوای وابسته به آن حذف شود؟`}><input type="hidden" name="id" value={page.id}/><button className="admin-danger-button">حذف کامل صفحه</button></ActionForm></section></main>}
