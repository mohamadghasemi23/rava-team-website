import Link from'next/link';import{redirect}from'next/navigation';import{getTenantContext}from'@/lib/platform/tenant-context';import{canEditContent}from'@/lib/platform/capabilities';import ActionForm from'../components/ActionForm';import{createPage,deletePage,setPageStatus}from'./actions';export const dynamic='force-dynamic'
export default async function PagesAdminPage(){const a=await getTenantContext();if(!canEditContent(a))redirect('/admin');const{data}=await a.s.from('pages').select('id,title,slug,status,updated_at').eq('tenant_id',a.tenantId).order('updated_at',{ascending:false});const pages=data??[];return <main className="admin-shell"><header className="admin-head"><div><span>RAVA CONTROL CENTER · {a.tenant.name}</span><h1>مدیریت صفحات</h1><p>فقط صفحات سایت فعال نمایش داده می‌شوند.</p></div><div className="admin-actions"><Link className="admin-link" href="/admin">داشبورد</Link></div></header><section className="admin-panel"><h2>ساخت صفحه جدید</h2><ActionForm action={createPage} className="admin-form admin-form-inline" confirmTitle="ساخت صفحه جدید" confirmMessage="صفحه در سایت فعال ساخته شود؟" confirmLabel="بله، صفحه ساخته شود"><label>عنوان<input name="title" required placeholder="مثلاً خدمات طراحی سایت"/></label><label>آدرس صفحه<input name="slug" required dir="ltr" placeholder="web-design"/></label><button type="submit">ساخت صفحه</button></ActionForm></section><section className="admin-panel"><div className="admin-section-title"><h2>همه صفحات</h2><span>{pages.length.toLocaleString('fa-IR')} صفحه</span></div>{pages.length===0?<div className="admin-empty">هنوز صفحه‌ای ساخته نشده.</div>:<div className="admin-list">{pages.map(page=>{const pub=page.status==='published';return <article className="admin-list-item" key={page.id}><div className="admin-list-main"><div><b>{page.title}</b><small dir="ltr">/{page.slug}</small></div><span className={`status-pill status-${page.status}`}>{page.status}</span></div><div className="admin-row-actions"><Link className="admin-link" href={`/admin/pages/${page.id}`}>ویرایش</Link><ActionForm action={setPageStatus} confirmTitle={pub?'مخفی کردن صفحه':'انتشار صفحه'} confirmMessage={pub?`صفحه «${page.title}» مخفی شود؟`:`صفحه «${page.title}» منتشر شود؟`}><input type="hidden" name="id" value={page.id}/><input type="hidden" name="status" value={pub?'hidden':'published'}/><button className="admin-muted-button">{pub?'مخفی کن':'منتشر کن'}</button></ActionForm><ActionForm action={deletePage} danger confirmTitle="حذف کامل صفحه" confirmMessage={`صفحه «${page.title}» حذف شود؟`}><input type="hidden" name="id" value={page.id}/><button className="admin-danger-button">حذف</button></ActionForm></div></article>})}</div>}</section></main>}
