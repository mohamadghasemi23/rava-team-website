import Link from 'next/link';import {createClient} from '@/lib/supabase/server';import {requirePermission} from '@/lib/security/permissions';import './logs.css'
export const dynamic='force-dynamic';const PAGE_SIZE=25
const CATEGORIES=[['error','خطاها','خطاهای Runtime، دیتابیس، Storage یا سرویس‌ها که نیاز به بررسی دارند.'],['security','امنیت','رویدادهای مشکوک، محدودسازی درخواست‌ها و کنترل‌های امنیتی.'],['auth','ورود و نشست','ورود، خروج، CAPTCHA، Session و احراز هویت مدیران.'],['audit','تغییرات مدیریتی','ساخت، ویرایش و حذف اطلاعات مهم CMS؛ به‌صورت خودکار از دیتابیس.'],['system','سیستم','رویدادهای داخلی و عملیاتی برنامه و سرویس‌ها.'],['performance','کارایی','رویدادهای مربوط به کندی، Timeout و مصرف منابع.']] as const
function href(params:Record<string,string|number|undefined>){const s=new URLSearchParams();Object.entries(params).forEach(([k,v])=>{if(v!==undefined&&v!==''&&v!=='all')s.set(k,String(v))});return`?${s.toString()}`};function safeDate(v:string|undefined){if(!v)return null;const d=new Date(v);return Number.isNaN(d.getTime())?null:d}
export default async function LogsPage({searchParams}:{searchParams:Promise<{q?:string;view?:string;category?:string;severity?:string;source?:string;from?:string;to?:string;page?:string}>}){const access=await requirePermission('logs.view'),params=await searchParams,s=await createClient(),page=Math.max(1,Number(params.page||1)||1),q=(params.q||'').trim().slice(0,180),view=['all','errors','events'].includes(params.view||'')?params.view!:'all';let category=params.category||'all';const severity=params.severity||'all',source=params.source||'all',fromDate=safeDate(params.from),toDate=safeDate(params.to),canErrors=access.permissions.has('errors.view'),canSecurity=access.permissions.has('security_logs.view'),canAudit=access.permissions.has('audit_logs.view');if(view==='errors'&&!canErrors)category='__denied__';if(category==='security'&&!canSecurity)category='__denied__';if(category==='audit'&&!canAudit)category='__denied__';if(category==='error'&&!canErrors)category='__denied__';let query=s.from('system_events').select('event_no,event_id,request_id,category,severity,event_name,message,summary_fa,cause_fa,route,method,actor_user_id,actor_role,source,http_status,error_name,error_code,metadata,created_at',{count:'exact'}).eq('tenant_id',access.tenantId).order('created_at',{ascending:false});if(q){if(/^\d+$/.test(q))query=query.eq('event_no',Number(q));else query=query.or(`event_id.ilike.%${q}%,request_id.ilike.%${q}%,event_name.ilike.%${q}%,message.ilike.%${q}%,summary_fa.ilike.%${q}%,cause_fa.ilike.%${q}%,route.ilike.%${q}%,error_code.ilike.%${q}%`)}if(view==='errors')query=query.eq('category','error');else if(view==='events')query=query.neq('category','error');if(category!=='all')query=query.eq('category',category);if(severity!=='all')query=query.eq('severity',severity);if(source!=='all')query=query.eq('source',source);if(fromDate)query=query.gte('created_at',fromDate.toISOString());if(toDate){const end=new Date(toDate);end.setHours(23,59,59,999);query=query.lte('created_at',end.toISOString())}const from=(page-1)*PAGE_SIZE,{data,count,error}=await query.range(from,from+PAGE_SIZE-1),total=count||0,totalPages=Math.max(1,Math.ceil(total/PAGE_SIZE)),base={q,view,category,severity,source,from:params.from,to:params.to},visibleCategories=CATEGORIES.filter(([k])=>k!=='security'||canSecurity).filter(([k])=>k!=='audit'||canAudit).filter(([k])=>k!=='error'||canErrors);return <main className="admin-shell"><header className="admin-head"><div><span>RAVA OBSERVABILITY · TENANT</span><h1>مرکز لاگ و خطا</h1><p>{access.tenant.name} · فقط رویدادهای همین سایت</p></div><div className="admin-actions">{access.isSuperAdmin||access.isPlatformAdmin?<Link className="admin-link" href="/admin/access">مدیریت دسترسی‌ها</Link>:null}<Link className="admin-link" href="/admin">داشبورد</Link></div></header><section className="admin-log-tabs"><Link className={view==='all'?'is-active':''} href={href({...base,view:'all',page:1})}>همه مجاز</Link>{canErrors?<Link className={view==='errors'?'is-active':''} href={href({...base,view:'errors',page:1})}>فقط خطاها</Link>:null}<Link className={view==='events'?'is-active':''} href={href({...base,view:'events',page:1})}>فقط رویدادها</Link></section><section className="admin-log-category-grid">{visibleCategories.map(([key,title,description])=><Link key={key} className={`admin-log-category ${category===key?'is-active':''}`} href={href({...base,category:key,page:1})}><b>{title}</b><small>{description}</small></Link>)}</section><section className="admin-panel"><div className="admin-section-title"><div><h2>جستجو و فیلتر</h2><p>شماره عددی، شناسه رویداد، Request ID، مسیر، کد خطا یا عبارت فارسی.</p></div><Link className="admin-muted-button" href="/admin/logs">پاک کردن فیلترها</Link></div><form className="admin-log-filters" method="get"><input type="hidden" name="view" value={view}/><label className="admin-log-search">جستجو<input name="q" defaultValue={q} dir="auto"/></label><label>دسته<select name="category" defaultValue={category==='__denied__'?'all':category}><option value="all">همه دسته‌های مجاز</option>{visibleCategories.map(([key,title])=><option key={key} value={key}>{title}</option>)}</select></label><label>شدت<select name="severity" defaultValue={severity}><option value="all">همه</option><option value="critical">بحرانی</option><option value="error">خطا</option><option value="warning">هشدار</option><option value="info">اطلاعات</option><option value="debug">Debug</option></select></label><label>منبع<select name="source" defaultValue={source}><option value="all">همه</option><option value="server">Server</option><option value="database">Database</option><option value="client">Client</option><option value="proxy">Proxy</option></select></label><label>از<input name="from" type="date" defaultValue={params.from||''}/></label><label>تا<input name="to" type="date" defaultValue={params.to||''}/></label><button className="admin-primary-button">اعمال</button></form></section><section className="admin-panel"><div className="admin-section-title"><div><h2>لاگ‌های قابل مشاهده</h2><p>Tenant Isolation فعال است.</p></div><span>{total.toLocaleString('fa-IR')} رکورد</span></div>{error?<div className="admin-empty">خواندن لاگ‌ها با خطا مواجه شد.</div>:!(data||[]).length?<div className="admin-empty">رکوردی پیدا نشد.</div>:<div className="admin-log-list">{(data||[]).map((e:any)=><article className={`admin-log-row severity-${e.severity}`} key={e.event_id}><div className="admin-log-row-head"><div><span className="admin-log-number">#{e.event_no}</span><code dir="ltr">{e.event_id}</code></div><time>{new Date(e.created_at).toLocaleString('fa-IR')}</time></div><div className="admin-log-badges"><span>{CATEGORIES.find(x=>x[0]===e.category)?.[1]||e.category}</span><span>{e.severity}</span><span>{e.source}</span></div><h3>{e.summary_fa||e.message||e.event_name}</h3>{e.cause_fa?<div className="admin-log-cause"><b>علت / توضیح:</b><p>{e.cause_fa}</p></div>:null}<div className="admin-log-tech"><code dir="ltr">{e.event_name}</code>{e.error_name?<code dir="ltr">{e.error_name}{e.error_code?` · ${e.error_code}`:''}</code>:null}{e.route?<code dir="ltr">{e.method||''} {e.route}</code>:null}{e.request_id?<code dir="ltr">Request: {e.request_id}</code>:null}</div><details><summary>Metadata امن</summary><pre dir="ltr">{JSON.stringify(e.metadata||{},null,2)}</pre></details></article>)}</div>}<div className="admin-pagination"><Link className={`admin-link ${page<=1?'is-disabled':''}`} href={href({...base,page:Math.max(1,page-1)})}>قبل</Link><span>صفحه {page.toLocaleString('fa-IR')} از {totalPages.toLocaleString('fa-IR')}</span><Link className={`admin-link ${page>=totalPages?'is-disabled':''}`} href={href({...base,page:Math.min(totalPages,page+1)})}>بعد</Link></div></section></main>}
