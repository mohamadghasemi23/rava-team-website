import Link from 'next/link';import {redirect} from 'next/navigation';import {getTenantContext} from '@/lib/platform/tenant-context';import MediaManager from './MediaManager';import VideoUploader from './VideoUploader'
export const dynamic='force-dynamic';const PAGE_SIZE=24
export default async function MediaAdminPage(){const a=await getTenantContext();if(!['super_admin','admin','content_manager'].includes(a.profile.role)&&!a.isPlatform)redirect('/admin');const{data,count}=await a.s.from('media_assets').select('id,storage_path,file_name,mime_type,alt_text,size_bytes,created_at,folder,title,description,caption,credit',{count:'exact'}).eq('tenant_id',a.tenantId).is('deleted_at',null).order('created_at',{ascending:false}).range(0,PAGE_SIZE-1);return <main className="admin-shell"><header className="admin-head"><div><span>RAVA MEDIA · TENANT</span><h1>رسانه‌ها</h1><p>{a.tenant.name} · کتابخانه مستقل همین سایت</p></div><div className="admin-actions"><Link className="admin-link" href="/admin/media/trash">سطل زباله</Link><Link className="admin-link" href="/admin">داشبورد</Link></div></header><VideoUploader userId={a.userId} tenantId={a.tenantId}/><MediaManager initialAssets={data??[]} initialTotal={count??0} userId={a.userId} pageSize={PAGE_SIZE}/></main>}
