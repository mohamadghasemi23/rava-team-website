'use client'
import {ChangeEvent,useMemo,useState} from 'react';import {createClient} from '@/lib/supabase/client'
export default function VideoUploader({userId}:{userId:string}){const s=useMemo(()=>createClient(),[]),[file,setFile]=useState<File|null>(null),[title,setTitle]=useState(''),[caption,setCaption]=useState(''),[busy,setBusy]=useState(false),[confirm,setConfirm]=useState(false),[message,setMessage]=useState('');function pick(e:ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0]||null;if(!f)return;const ok=['video/mp4','video/webm','video/quicktime'].includes(f.type);if(!ok){setMessage('فقط MP4، WebM یا MOV مجاز است.');return}if(f.size>250*1024*1024){setMessage('حجم ویدیو باید کمتر از ۲۵۰ مگابایت باشد.');return}setFile(f);setTitle(v=>v||f.name.replace(/\.[^.]+$/,''));setMessage('')};async function upload(){if(!file)return;setConfirm(false);setBusy(true);const safe=file.name.toLowerCase().replace(/[^a-z0-9._-]+/g,'-'),path=`${userId}/${Date.now()}-${crypto.randomUUID()}-${safe}`;const up=await s.storage.from('rava-media').upload(path,file,{cacheControl:'31536000',contentType:file.type});if(up.error){setBusy(false);setMessage(`آپلود انجام نشد: ${up.error.message}`);return}const ins=await s.from('media_assets').insert({storage_path:path,file_name:file.name,mime_type:file.type,alt_text:title.trim(),title:title.trim(),caption:caption.trim(),folder:'projects',size_bytes:file.size,uploaded_by:userId,metadata:{media_kind:'video'}});if(ins.error){await s.storage.from('rava-media').remove([path]);setBusy(false);setMessage(`ثبت ویدیو انجام نشد: ${ins.error.message}`);return}setBusy(false);setFile(null);setTitle('');setCaption('');setMessage('ویدیو با موفقیت ثبت شد. صفحه را Refresh کن تا در کتابخانه دیده شود.')}return <section className="admin-panel"><h2>آپلود ویدیو</h2><p>برای نمونه‌کارها؛ MP4/WebM/MOV تا ۲۵۰ مگابایت. برای فایل‌های بزرگ‌تر بعداً Video CDN/Streaming استفاده می‌کنیم.</p><div className="admin-form"><label>فایل ویدیو<input type="file" accept="video/mp4,video/webm,video/quicktime" onChange={pick}/></label><label>عنوان<input value={title} onChange={e=>setTitle(e.target.value)} maxLength={180}/></label><label>Caption<textarea value={caption} onChange={e=>setCaption(e.target.value)} maxLength={1200}/></label>{message?<p>{message}</p>:null}<button type="button" className="admin-primary-button" disabled={!file||busy} onClick={()=>setConfirm(true)}>{busy?'در حال آپلود…':'آپلود ویدیو'}</button></div>{confirm?<div className="admin-modal-backdrop" onMouseDown={()=>setConfirm(false)}><div className="admin-modal" onMouseDown={e=>e.stopPropagation()}><h3>تأیید آپلود ویدیو</h3><p>ویدیو «{file?.name}» روی سرور ثبت شود؟</p><div className="admin-modal-actions"><button className="admin-primary-button" type="button" onClick={upload}>بله، آپلود شود</button><button className="admin-muted-button" type="button" onClick={()=>setConfirm(false)}>انصراف</button></div></div></div>:null}</section>}
