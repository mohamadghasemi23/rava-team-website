import Link from 'next/link';import {redirect} from 'next/navigation';import {getTenantContext} from '@/lib/platform/tenant-context';import {canEditContent} from '@/lib/platform/capabilities';import TrashManager from './TrashManager'
export const dynamic='force-dynamic'
export default async function MediaTrashPage(){const a=await getTenantContext();if(!canEditContent(a))redirect('/admin');const{data}=await a.s.from('media_assets').select('id,storage_path,file_name,mime_type,alt_text,size_bytes,deleted_at').eq('tenant_id',a.tenantId).not('deleted_at','is',null).order('deleted_at',{ascending:false}).limit(100);return <main className="admin-shell"><header className="admin-head"><div><span>RAVA MEDIA TRASH · {a.tenant.name}</span><h1>سطل زباله رسانه‌ها</h1></div><Link className="admin-link" href="/admin/media">بازگشت به رسانه‌ها</Link></header><TrashManager initialAssets={data??[]} tenantId={a.tenantId}/></main>}
