import{themeLibraryMeta}from'@/lib/theme/library'
export default function ThemeGallery({themes,allowed,current}:{themes:any[];allowed:Record<string,boolean>;current?:string}){return <div className="theme-gallery">{themes.map(t=>{const m=themeLibraryMeta(t.theme_key),ok=allowed[t.theme_key]!==false;return <article className={`theme-gallery-card ${m.previewClass} ${current===t.theme_key?'is-current':''}`} key={t.theme_key}><div className="theme-gallery-visual"><span>{m.family}</span><strong>{t.name_en}</strong><i/><i/><i/></div><div className="theme-gallery-copy"><div><b>{t.name_fa}</b><small>v{t.version} · {m.toneFa}</small></div><p>{m.audienceFa}</p><div className="theme-gallery-badges"><span>{current===t.theme_key?'فعال':'Theme'}</span>{m.premium&&<span>{ok?'فعال برای قرارداد':'قفل قراردادی'}</span>}</div>{!ok&&<p className="theme-gallery-lock">این قالب در قرارداد این مشتری فعال نشده است. فعال‌سازی فقط از Control Center مرکزی انجام می‌شود.</p>}</div></article>})}</div>}
