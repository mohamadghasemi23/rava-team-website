import{getTenantContext}from'@/lib/platform/tenant-context';import{hasEntitlement}from'@/lib/platform/entitlements';import HelpIcon from'@/app/admin/components/HelpIcon';import{redirect}from'next/navigation';import{saveCheckoutField,saveCheckoutSettings}from'./actions';export const dynamic='force-dynamic';
export default async function CheckoutAdmin(){const a=await getTenantContext();if(!await hasEntitlement(a.s,a.tenantId,'commerce.core'))redirect('/admin/commerce');const[{data:settings},{data:fields}]=await Promise.all([a.s.from('checkout_settings').select('*').eq('tenant_id',a.tenantId).maybeSingle(),a.s.from('checkout_fields').select('*').eq('tenant_id',a.tenantId).order('sort_order')]);return <main className="admin-shell"><header className="admin-head"><div><span>COMMERCE · CHECKOUT</span><h1>اطلاعات قبل از پرداخت <HelpIcon helpKey="checkout.overview"/></h1><p>مشخص کن مشتری قبل از رفتن به درگاه چه اطلاعاتی وارد کند و کدام‌ها اجباری باشند.</p></div></header><section className="admin-panel"><h2>نوع Checkout</h2><form action={saveCheckoutSettings} className="admin-form"><label>نوع فروش<select name="checkout_mode" defaultValue={settings?.checkout_mode||'physical'}><option value="physical">کالای فیزیکی</option><option value="digital">محصول دیجیتال</option><option value="service">خدمات</option><option value="hybrid">ترکیبی</option></select></label><label><input type="checkbox" name="require_shipping" defaultChecked={settings?.require_shipping??true}/> دریافت اطلاعات ارسال</label><label><input type="checkbox" name="terms_required" defaultChecked={settings?.terms_required??true}/> پذیرش قوانین قبل از پرداخت</label><button className="admin-primary-button">ذخیره تنظیمات</button></form></section><section className="admin-panel"><h2>فیلدهای مشتری <HelpIcon helpKey="checkout.fields"/></h2><p>برای امنیت، نوع اعتبارسنجی هر فیلد از لیست امن انتخاب می‌شود و مدیر نمی‌تواند کد یا Regex دلخواه وارد کند.</p><div className="admin-log-list">{(fields||[]).map((f:any)=><article className="admin-log-row" key={f.id}><form action={saveCheckoutField} className="admin-form"><input type="hidden" name="id" value={f.id}/><div className="admin-grid-2"><label>عنوان فارسی<input name="label_fa" defaultValue={f.label_fa} maxLength={120} required/></label><label>English label<input name="label_en" defaultValue={f.label_en} maxLength={120}/></label><label>حداکثر طول<input name="max_length" type="number" min="1" max="2000" defaultValue={f.max_length||500}/></label><label>اعتبارسنجی<select name="validation_rule" defaultValue={f.validation_rule}><option value="plain">متن معمولی</option><option value="name">نام</option><option value="email">ایمیل</option><option value="phone">تلفن</option><option value="postal_code">کد پستی</option><option value="address">آدرس</option><option value="national_id">شناسه ملی</option><option value="company_id">شناسه شرکت</option></select></label></div><label><input type="checkbox" name="enabled" defaultChecked={f.enabled}/> نمایش داده شود</label><label><input type="checkbox" name="required" defaultChecked={f.required}/> اجباری باشد</label><button className="admin-muted-button">ذخیره این فیلد</button></form></article>)}</div></section></main>}
