import Link from'next/link';import{getTenantContext}from'@/lib/platform/tenant-context';import HelpIcon from'@/app/admin/components/HelpIcon';export const dynamic='force-dynamic';export default async function SearchAdmin(){const a=await getTenantContext();if(a.platformWide)throw new Error('tenant_required');const{data:active}=await a.s.rpc('has_entitlement',{p_tenant:a.tenantId,p_feature:'search.pro'});if(!active)return <main className="admin-shell"><header className="admin-head"><div><span>COMMERCE · SEARCH</span><h1>Search Pro 🔒</h1><p>این بخش در قرارداد فعلی فعال نیست؛ اما می‌توانی امکاناتی که به فروشگاه اضافه می‌کند را ببینی.</p></div><Link className="admin-primary-button" href="/admin/upgrades">مشاهده و درخواست فعال‌سازی</Link></header><section className="upgrade-grid"><article className="upgrade-card is-locked"><h2>تحلیل تقاضای مشتری</h2><p>پرکاربردترین جستجوها، عبارت‌های بدون نتیجه و فرصت‌هایی که فروشگاه از دست می‌دهد.</p></article><article className="upgrade-card is-locked"><h2>Autocomplete هوشمند</h2><p>پیشنهاد محصول و دسته در زمان تایپ برای رسیدن سریع‌تر مشتری به کالا.</p></article><article className="upgrade-card is-locked"><h2>Synonym</h2><p>عبارت‌های مختلف مثل «تی‌شرت / تیشرت / T-shirt» می‌توانند یک مفهوم شناخته شوند.</p></article><article className="upgrade-card is-locked"><h2>Ranking & Merchandising</h2><p>Boost و Pin محصولات برای کنترل تجاری ترتیب نمایش نتایج.</p></article></section></main>;const{data:d,error}=await a.s.rpc('search_pro_dashboard',{p_tenant:a.tenantId,p_days:30});if(error)throw error;const top=(d?.top_queries||[])as any[],zero=(d?.zero_queries||[])as any[];return <main className="admin-shell"><header className="admin-head"><div><span>COMMERCE · SEARCH PRO · 30 DAYS</span><h1>Search Pro <HelpIcon helpKey="search.pro.analytics"/></h1><p>ببین مشتری‌ها دنبال چه چیزی هستند و کجا بدون نتیجه از جستجو خارج می‌شوند.</p></div></header><section className="upgrade-summary"><div><b>{Number(d?.searches||0).toLocaleString('fa-IR')}</b><span>جستجو</span></div><div><b>{Number(d?.zero_results||0).toLocaleString('fa-IR')}</b><span>بدون نتیجه</span></div><div><b>{Number(d?.zero_rate||0).toLocaleString('fa-IR')}٪</b><span>نرخ بدون نتیجه</span></div></section><section className="admin-grid-2"><article className="admin-panel"><h2>پرجستجوترین عبارت‌ها</h2>{top.length?top.map((x:any)=><div className="admin-log-row" key={x.query}><b>{x.query}</b><span>{Number(x.searches).toLocaleString('fa-IR')} جستجو</span></div>):<p>هنوز داده کافی نداریم.</p>}</article><article className="admin-panel"><h2>فرصت‌های بدون نتیجه</h2>{zero.length?zero.map((x:any)=><div className="admin-log-row" key={x.query}><b>{x.query}</b><span>{Number(x.searches).toLocaleString('fa-IR')} بار بدون نتیجه</span></div>):<p>عالیه؛ فعلاً عبارت پرتکرار بدون نتیجه نداریم.</p>}</article></section><section className="admin-panel"><h2>کنترل Search Pro <HelpIcon helpKey="search.pro.merchandising"/></h2><p>زیرساخت Synonym، Boost و Pin محصول فعال است. رابط مدیریت قوانین در مرحله بعد به همین صفحه اضافه می‌شود.</p></section></main>}
