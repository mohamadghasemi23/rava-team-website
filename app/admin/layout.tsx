import './admin-media.css';import './admin-shell.css';import AdminSidebar from './components/AdminSidebar';import {getAdminAccess} from '@/lib/security/permissions'
export default async function AdminLayout({children}:{children:React.ReactNode}){const a=await getAdminAccess(),canLogs=a.permissions.has('logs.view'),canErrors=a.permissions.has('errors.view'),canAnalytics=['super_admin','admin'].includes(a.profile.role)||a.isPlatformAdmin,isPlatformOwner=a.platformRole==='platform_owner';return <div className="admin-app"><AdminSidebar tenantName={a.tenant.name} isSuperAdmin={a.isSuperAdmin} isPlatformAdmin={a.isPlatformAdmin} isPlatformOwner={isPlatformOwner} canLogs={canLogs} canErrors={canErrors} canAnalytics={canAnalytics} locale="fa"/><div className="admin-app-content">{children}</div></div>}
