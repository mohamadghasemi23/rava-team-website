import './admin-media.css';import './admin-shell.css';import './admin-theme.css';import AdminSidebar from './components/AdminSidebar';import {getAdminAccess} from '@/lib/security/permissions';import {canEditContent,canViewAnalytics} from '@/lib/platform/capabilities';import{adminSkinCssVars}from'@/lib/theme/runtime'
export default async function AdminLayout({children}:{children:React.ReactNode}){const a=await getAdminAccess(),canLogs=a.permissions.has('logs.view'),canErrors=a.permissions.has('errors.view'),canAnalytics=canViewAnalytics(a),canContent=canEditContent(a),isPlatformOwner=a.platformRole==='platform_owner',canAppearance=a.isPlatform||['super_admin','admin'].includes(a.profile.role),{data:theme}=await a.s.from('tenant_theme_settings').select('published_tokens,admin_skin_sync').eq('tenant_id',a.tenant.id).maybeSingle(),skin=theme?.admin_skin_sync?adminSkinCssVars((theme.published_tokens||{})as any):undefined;return <div className="admin-app" style={skin}><AdminSidebar tenantName={a.tenant.name} isSuperAdmin={a.isSuperAdmin} isPlatformAdmin={a.isPlatformAdmin} isPlatformOwner={isPlatformOwner} canContent={canContent} canAppearance={canAppearance} canLogs={canLogs} canErrors={canErrors} canAnalytics={canAnalytics} locale="fa"/><div className="admin-app-content">{children}</div></div>}
