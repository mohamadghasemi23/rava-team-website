import './admin-media.css';import './admin-shell.css';import AdminSidebar from './components/AdminSidebar';import {getAdminAccess} from '@/lib/security/permissions';import {canEditContent,canViewAnalytics} from '@/lib/platform/capabilities'
export default async function AdminLayout({children}:{children:React.ReactNode}){const a=await getAdminAccess(),canLogs=a.permissions.has('logs.view'),canErrors=a.permissions.has('errors.view'),canAnalytics=canViewAnalytics(a),canContent=canEditContent(a),isPlatformOwner=a.platformRole==='platform_owner';return <div className="admin-app"><AdminSidebar tenantName={a.tenant.name} isSuperAdmin={a.isSuperAdmin} isPlatformAdmin={a.isPlatformAdmin} isPlatformOwner={isPlatformOwner} canContent={canContent} canLogs={canLogs} canErrors={canErrors} canAnalytics={canAnalytics} locale="fa"/><div className="admin-app-content">{children}</div></div>}
