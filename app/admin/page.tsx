import Link from 'next/link';import {getAdminAccess} from '@/lib/security/permissions';import {canEditContent,canViewAnalytics} from '@/lib/platform/capabilities'
export const dynamic='force-dynamic'
export default async function AdminPage(){const a=await getAdminAccess(),content=canEditContent(a),analytics=canViewAnalytics(a);const[pages,projects,leads,media]=await Promise.all([a.s.from('pages').select('*',{count:'exact',head:true}).eq('tenant_id',a.tenantId),a.s.from('projects').select('*',{count:'exact',head:true}).eq('tenant_id',a.tenantId),a.s.from('leads').select('*',{count:'exact',head:true}).eq('tenant_id',a.tenantId),a.s.from('media_assets').select('*',{count:'exact',head:true}).eq('tenant_id',a.tenantId).is('deleted_at',null)]);const card=(href:string|undefined,label:string,count:number|null,small:string)=>(href?<Link href={href}><article><span>{label}</span><b>{count??0}</b><small>{small}</small></article></Link>:<article><span>{label}</span><b>{count??0}</b><small>فقط مشاهده اطلاعات کلی</small></article>);return <main className="admin-shell"><header className="admin-head"><div><span>RAVA CONTROL CENTER · {a.tenant.name}</span><h1>داشبورد</h1><p>تمام اعداد این صفحه مربوط به Tenant فعال هستند.</p></div><div className="admin-user"><b>{a.profile.display_name}</b><small>{a.tenantRole||a.platformRole||a.profile.role}</small></div></header><section className="admin-stats" aria-label="آمار پنل">{card(content?'/admin/pages':undefined,'صفحات',pages.count,'مدیریت صفحات ←')}{card(content?'/admin/projects':undefined,'پروژه‌ها',projects.count,'مدیریت پروژه‌ها ←')}<article><span>پیام‌ها</span><b>{leads.count??0}</b><small>CRM / Leads</small></article>{card(content?'/admin/media':undefined,'رسانه‌ها',media.count,'مدیریت رسانه‌ها ←')}</section><section className="admin-panel"><h2>Tenant Isolation فعال است</h2><p>Pages، Projects، Media، Analytics، SEO، Logs، Users و Version History روی سایت فعال ایزوله شده‌اند.</p><div className="admin-actions">{content?<><Link className="admin-link" href="/admin/pages">صفحات</Link><Link className="admin-link" href="/admin/projects">پروژه‌ها</Link><Link className="admin-link" href="/admin/media">رسانه‌ها</Link></>:null}<Link className="admin-link" href="/admin/seo">SEO</Link>{analytics?<Link className="admin-link" href="/admin/analytics">آمار</Link>:null}{a.isPlatformAdmin?<Link className="admin-primary-button" href="/admin/platform">RAVA Control Plane</Link>:null}</div></section><form action="/auth/signout" method="post"><button className="admin-signout" type="submit">خروج از پنل</button></form></main>}
