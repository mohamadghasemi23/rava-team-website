import ActionForm from'@/app/admin/components/ActionForm';import HelpIcon from'@/app/admin/components/HelpIcon';import{getTenantContext}from'@/lib/platform/tenant-context';import{generateStarterContent,cleanupCommerceDemo}from'./actions';export const dynamic='force-dynamic'
export default async function StartPage(){const a=await getTenantContext();const[{data:run},{data:demo}]=await Promise.all([a.s.from('starter_content_runs').select('*').eq('tenant_id',a.tenantId).order('started_at',{ascending:false}).limit(1).maybeSingle(),a.s.from('starter_demo_batches').select('id,kind,status,created_at').eq('tenant_id',a.tenantId).eq('status','active').order('created_at')]);const demoKinds=new Set((demo||[]).map((x:any)=>x.kind));return <main className="admin-shell"><header className="admin-head"><div><span>RAVA START · {a.tenant.name}</span><h1>شروع هوشمند سایت <HelpIcon helpKey="starter.overview"/></h1><p>سایت و هر ماژول قراردادی فعال با داده آموزشی قابل لمس شروع می‌شود؛ بدون دست‌زدن به محتوای واقعی.</p></div></header><section className="admin-grid-2"><article className="admin-panel"><span className="kicker">SITE ARCHETYPE</span><h2>{a.tenant.site_archetype}</h2><p>بسته شروع بر اساس نوع سایت و قابلیت‌هایی که در قرارداد فعال شده‌اند ساخته می‌شود.</p></article><article className="admin-panel"><span className="kicker">STARTER STATUS</span><h2>{run?.status||'Not started'}</h2><p>{run?.status==='completed'?'بسته آموزشی ساخته شده و آماده ویرایش است.':'هنوز بسته شروع برای این سایت ساخته نشده است.'}</p></article></section><section className="admin-panel"><div className="admin-section-title"><div><h2>Start Content Pack <HelpIcon helpKey="starter.generate"/></h2><p>RAVA از داده‌های کنترل‌شده و معنی‌دار استفاده می‌کند؛ نه Lorem Ipsum و نه داده تصادفی بی‌معنی.</p></div></div><div className="admin-grid-2"><div><h3>چه چیزهایی ساخته می‌شود؟</h3><p>صفحات پایه، Blockهای آموزشی، پروژه نمونه و صفحه اصلی. اگر فروشگاه در قرارداد فعال باشد، محصول، دسته، تخفیف و Review نمونه هم ساخته می‌شود. Inventory Pro و Procurement Pro هم فقط در صورت فعال بودن قرارداد، داده آموزشی خودشان را می‌گیرند.</p></div><div><h3>Demo از داده واقعی جداست</h3><p>تمام داده‌های نمونه با Demo Batch مشخص می‌شوند، در گزارش مالی واقعی قابل حذف هستند و پاک‌سازی فقط همان رکوردهای آموزشی را هدف می‌گیرد.</p></div></div><ActionForm action={generateStarterContent} confirmTitle="ساخت محتوای شروع" confirmMessage={`برای «${a.tenant.name}» محتوای نمونه و Demo ماژول‌های قراردادی ساخته شود؟ اطلاعات واقعی موجود overwrite نمی‌شوند.`}><button className="admin-primary-button" disabled={run?.status==='completed'}>{run?.status==='completed'?'Starter Content ساخته شده':'Start · ساخت محتوای نمونه'}</button></ActionForm></section><section className="admin-panel"><h2>Demo ماژول‌ها</h2><div className="admin-grid-2"><div><h3>فروشگاه</h3><p>{demoKinds.has('commerce')?'Demo فعال: محصول، تخفیف، سفارش و Review نمونه':'Demo فعال نیست'}</p></div><div><h3>انبارداری</h3><p>{demoKinds.has('inventory')?'Demo فعال: انبار، موجودی، رزرو و گردش نمونه':'Demo فعال نیست یا در قرارداد نیست'}</p></div><div><h3>تأمین و خرید</h3><p>{demoKinds.has('procurement')?'Demo فعال: تأمین‌کننده و سفارش خرید نمونه':'Demo فعال نیست یا در قرارداد نیست'}</p></div><div><h3>پاک‌سازی امن</h3><p>وقتی آموزش تمام شد، فقط داده‌های Demo را حذف کن؛ داده‌های واقعی مشتری باقی می‌مانند.</p></div></div>{(demo||[]).length?<ActionForm action={cleanupCommerceDemo} confirmTitle="پاک کردن داده‌های Demo" confirmMessage="تمام داده‌های نمونه فروشگاه، انبار و تأمین حذف شوند؟ داده‌های واقعی حذف نمی‌شوند."><button className="admin-muted-button">پاک‌سازی داده‌های Demo</button></ActionForm>:null}</section><section className="admin-panel"><h2>چرا این کار مهم است؟</h2><p>مدیر از روز اول با سیستم خالی روبه‌رو نمی‌شود. خودش محصول، سفارش، موجودی و تأمین‌کننده نمونه را می‌بیند و همان‌جا مفهوم هر بخش را یاد می‌گیرد؛ بعد با یک پاک‌سازی امن وارد کار واقعی می‌شود.</p></section></main>}
