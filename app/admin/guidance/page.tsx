import{getTenantContext}from'@/lib/platform/tenant-context';import{saveGuidancePreferences}from'./actions';import HelpIcon from'@/app/admin/components/HelpIcon';export const dynamic='force-dynamic'
export default async function GuidanceCenter({searchParams}:{searchParams:Promise<{q?:string}>}){const a=await getTenantContext(),sp=await searchParams,q=(sp.q||'').trim().toLowerCase();const[{data:articles},{data:prefs}]=await Promise.all([a.s.from('guidance_articles').select('*').eq('active',true).order('sort_order'),a.s.from('guidance_user_preferences').select('*').eq('user_id',a.userId).eq('tenant_id',a.tenantId).maybeSingle()]);const rows=(articles||[]).filter((x:any)=>!q||`${x.title_fa} ${x.title_en} ${x.summary_fa} ${x.summary_en} ${(x.keywords||[]).join(' ')}`.toLowerCase().includes(q));return <main className="admin-shell"><header className="admin-head"><div><span>RAVA GUIDANCE · {a.tenant.name}</span><h1>مرکز راهنما</h1><p>آموزش‌های کوتاه، جستجوپذیر و مرتبط با نقش شما؛ بدون نیاز به خروج از پنل.</p></div></header><section className="admin-panel"><form method="get" className="admin-form"><label>جستجو در آموزش‌ها<input name="q" defaultValue={sp.q||''} placeholder="مثلاً: تغییر عکس پروژه، سئو، قالب…"/></label><button className="admin-primary-button">جستجو</button></form></section><section className="admin-panel"><div className="admin-section-title"><div><h2>تنظیم تجربه کاربری <HelpIcon helpKey="guidance.experience_mode"/></h2><p>Basic برای کار روزمره ساده‌تر است؛ Advanced کنترل‌های حرفه‌ای‌تر را نشان می‌دهد.</p></div></div><form action={saveGuidancePreferences} className="admin-form"><div className="admin-grid-2"><label>زبان راهنما<select name="locale" defaultValue={prefs?.locale||'fa'}><option value="fa">فارسی</option><option value="en">English</option></select></label><label>سطح تجربه<select name="experience_mode" defaultValue={prefs?.experience_mode||'basic'}><option value="basic">Basic · ساده</option><option value="advanced">Advanced · حرفه‌ای</option></select></label></div><label><input type="checkbox" name="guidance_dock_enabled" defaultChecked={prefs?.guidance_dock_enabled??true}/> دکمه راهنمای سراسری فعال باشد</label><button className="admin-muted-button">ذخیره تنظیمات</button></form></section><section className="admin-panel"><div className="admin-section-title"><div><h2>آموزش‌ها</h2><p>{rows.length.toLocaleString('fa-IR')} نتیجه</p></div></div><div className="guidance-library">{rows.map((x:any)=><article key={x.article_key} className="guidance-library-card"><div><span className={`guidance-importance is-${x.importance}`}>{x.area}</span><h3>{(prefs?.locale||'fa')==='en'?x.title_en:x.title_fa}</h3><p>{(prefs?.locale||'fa')==='en'?x.body_en:x.body_fa}</p></div>{x.related_help_keys?.length?<small dir="ltr">{x.related_help_keys.join(' · ')}</small>:null}</article>)}</div></section></main>}
