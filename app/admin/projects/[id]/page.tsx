import Link from 'next/link';import {notFound,redirect} from 'next/navigation';import ActionForm from '@/app/admin/components/ActionForm';import ProjectForm from '../ProjectForm';import {deleteProject,updateProject} from '../actions';import {getTenantContext} from '@/lib/platform/tenant-context';import {canEditContent} from '@/lib/platform/capabilities'
export default async function EditProject({params}:{params:Promise<{id:string}>}){const{id}=await params,a=await getTenantContext();if(!canEditContent(a))redirect('/admin');const{data:p}=await a.s.from('projects').select('*').eq('id',id).eq('tenant_id',a.tenantId).single();if(!p)notFound();return <main className="admin-shell"><header className="admin-head"><div><span>PORTFOLIO CMS · {a.tenant.name}</span><h1>{p.title}</h1><code dir="ltr">/projects/{p.slug}</code></div><Link className="admin-link" href="/admin/projects">بازگشت</Link></header><section className="admin-panel"><ProjectForm action={updateProject} tenantId={a.tenantId} project={p}/></section><section className="admin-panel"><h2>منطقه خطر</h2><ActionForm action={deleteProject} danger confirmTitle="حذف پروژه" confirmMessage="این پروژه فقط از Tenant فعلی حذف شود؟"><input type="hidden" name="id" value={p.id}/><button className="admin-danger-button">حذف پروژه</button></ActionForm></section></main>}
