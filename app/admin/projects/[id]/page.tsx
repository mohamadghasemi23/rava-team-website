import Link from 'next/link';import { notFound } from 'next/navigation';import { createClient } from '@/lib/supabase/server';import ActionForm from '@/app/admin/components/ActionForm';import ProjectForm from '../ProjectForm';import { deleteProject,updateProject } from '../actions'
export default async function EditProject({params}:{params:Promise<{id:string}>}){const{id}=await params,s=await createClient(),{data:p}=await s.from('projects').select('*').eq('id',id).single();if(!p)notFound();return <main className="admin-shell"><header className="admin-head"><div><span>PORTFOLIO CMS</span><h1>{p.title}</h1><code dir="ltr">/projects/{p.slug}</code></div><Link className="admin-link" href="/admin/projects">بازگشت</Link></header><section className="admin-panel"><ProjectForm action={updateProject} project={p}/></section><section className="admin-panel"><h2>منطقه خطر</h2><ActionForm action={deleteProject} danger confirmTitle="حذف پروژه" confirmMessage="این پروژه حذف شود؟ این عملیات باید فقط در صورت اطمینان انجام شود."><input type="hidden" name="id" value={p.id}/><button className="admin-danger-button">حذف پروژه</button></ActionForm></section></main>}
