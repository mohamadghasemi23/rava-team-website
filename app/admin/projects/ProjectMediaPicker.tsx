'use client'
import {useEffect,useMemo,useState} from 'react';import {createClient} from '@/lib/supabase/client'
type Asset={id:string;storage_path:string;file_name:string;alt_text:string;title:string;folder:string;mime_type:string;caption?:string}
type Item={media_id:string;kind:'image'|'video';caption?:string;poster_media_id?:string|null}
const SIZE=18
export default function ProjectMediaPicker({name,multiple=false,defaultValue=[],mixed=false}:{name:string;multiple?:boolean;defaultValue?:string[]|Item[];mixed?:boolean}){const s=useMemo(()=>createClient(),[]),[open,setOpen]=useState(false),[assets,setAssets]=useState<Asset[]>([]),[selected,setSelected]=useState<Item[]>(()=>mixed?(defaultValue as Item[]):(defaultValue as string[]).map(id=>({media_id:id,kind:'image'}))),[selectedAssets,setSelectedAssets]=useState<Asset[]>([]),[q,setQ]=useState(''),[folder,setFolder]=useState('projects'),[type,setType]=useState('all'),[page,setPage]=useState(1),[total,setTotal]=useState(0),[loading,setLoading]=useState(false);const pages=Math.max(1,Math.ceil(total/SIZE)),url=(p:string)=>s.storage.from('rava-media').getPublicUrl(p).data.publicUrl;async function load(p=1){setLoading(true);let query=s.from('media_assets').select('id,storage_path,file_name,alt_text,title,folder,mime_type,caption',{count:'exact'}).is('deleted_at',null).order('created_at',{ascending:false});const clean=q.trim().replace(/[%_,()]/g,' ');if(clean)query=query.or(`file_name.ilike.%${clean}%,title.ilike.%${clean}%,alt_text.ilike.%${clean}%`);if(folder!=='all')query=query.eq('folder',folder);if(type==='image')query=query.like('mime_type','image/%');if(type==='video')query=query.like('mime_type','video/%');const from=(p-1)*SIZE,{data,count}=await query.range(from,from+SIZE-1);setAssets((data||[]) as Asset[]);setTotal(count||0);setPage(p);setLoading(false)}useEffect(()=>{const ids=selected.map(x=>x.media_id);if(!ids.length)return;s.from('media_assets').select('id,storage_path,file_name,alt_text,title,folder,mime_type,caption').in('id',ids).then(({data})=>{const m=new Map((data||[]).map((a:any)=>[a.id,a]));setSelectedAssets(ids.map(id=>m.get(id)).filter(Boolean) as Asset[])})},[]);function toggle(a:Asset){const kind=a.mime_type.startsWith('video/')?'video':'image';if(!mixed&&kind==='video')return;if(multiple){setSelected(v=>v.some(x=>x.media_id===a.id)?v.filter(x=>x.media_id!==a.id):[...v,{media_id:a.id,kind,caption:a.caption||''}].slice(0,60));setSelectedAssets(v=>v.some(x=>x.id===a.id)?v.filter(x=>x.id!==a.id):[...v,a].slice(0,60))}else{setSelected([{media_id:a.id,kind}]);setSelectedAssets([a]);setOpen(false)}}function remove(id:string){setSelected(v=>v.filter(x=>x.media_id!==id));setSelectedAssets(v=>v.filter(x=>x.id!==id))}function move(id:string,dir:-1|1){const i=selected.findIndex(x=>x.media_id===id),j=i+dir;if(i<0||j<0||j>=selected.length)return;const ns=[...selected];[ns[i],ns[j]]=[ns[j],ns[i]];setSelected(ns);setSelectedAssets(ns.map(x=>selectedAssets.find(a=>a.id===x.media_id)!).filter(Boolean))}function updateCaption(id:string,val:string){setSelected(v=>v.map(x=>x.media_id===id?{...x,caption:val}:x))}const hidden=mixed?JSON.stringify(selected):multiple?selected.map(x=>x.media_id).join(','):(selected[0]?.media_id||'');return <div className="admin-media-picker"><input type="hidden" name={name} value={hidden}/><div className="admin-media-grid">{selectedAssets.map((a,i)=>{const item=selected.find(x=>x.media_id===a.id)!;const isVideo=a.mime_type.startsWith('video/');return <article className="admin-media-card" key={a.id}>{isVideo?<video src={url(a.storage_path)} muted preload="metadata"/>:<img src={url(a.storage_path)} alt={a.alt_text||a.file_name}/>}<div className="admin-media-meta"><b>{a.title||a.file_name}</b><small>{isVideo?'ویدیو':'تصویر'} · ترتیب {i+1}</small></div>{mixed?<input value={item?.caption||''} onChange={e=>updateCaption(a.id,e.target.value)} placeholder="کپشن اختیاری"/>:null}<div className="admin-media-actions">{multiple?<><button type="button" className="admin-muted-button" onClick={()=>move(a.id,-1)}>↑</button><button type="button" className="admin-muted-button" onClick={()=>move(a.id,1)}>↓</button></>:null}<button type="button" className="admin-danger-button" onClick={()=>remove(a.id)}>حذف</button></div></article>})}</div><button type="button" className="admin-muted-button" onClick={()=>{setOpen(true);load(1)}}>{mixed?'انتخاب عکس / ویدیو':'انتخاب رسانه'}</button>{open?<div className="admin-modal-backdrop" onMouseDown={()=>setOpen(false)}><div className="admin-modal admin-media-modal" onMouseDown={e=>e.stopPropagation()}><div className="admin-section-title"><div><h3>{mixed?'انتخاب عکس و ویدیو':'انتخاب رسانه'}</h3><p>جستجو، فیلتر و انتخاب مرتب‌شونده از Media Library.</p></div><span>{total.toLocaleString('fa-IR')} فایل</span></div><div className="admin-media-picker-toolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="جستجو…"/><select value={folder} onChange={e=>setFolder(e.target.value)}><option value="all">همه پوشه‌ها</option><option value="projects">projects</option><option value="general">general</option><option value="hero">hero</option><option value="branding">branding</option></select><select value={type} onChange={e=>setType(e.target.value)}><option value="all">همه نوع‌ها</option><option value="image">تصویر</option>{mixed?<option value="video">ویدیو</option>:null}</select><button type="button" className="admin-primary-button" onClick={()=>load(1)}>اعمال</button></div>{loading?<div className="admin-empty">در حال بارگذاری…</div>:<div className="admin-media-grid admin-media-picker-grid">{assets.map(a=>{const isVideo=a.mime_type.startsWith('video/'),on=selected.some(x=>x.media_id===a.id);if(!mixed&&isVideo)return null;return <article key={a.id} className={`admin-media-card ${on?'is-selected':''}`}>{isVideo?<video src={url(a.storage_path)} muted preload="metadata"/>:<img src={url(a.storage_path)} alt={a.alt_text||a.file_name}/>}<div className="admin-media-meta"><b>{a.title||a.file_name}</b><small>{isVideo?'ویدیو':'تصویر'} · {a.folder}</small></div><button type="button" className={on?'admin-muted-button':'admin-primary-button'} onClick={()=>toggle(a)}>{on?'حذف از انتخاب':'انتخاب'}</button></article>})}</div>}<div className="admin-pagination"><button type="button" className="admin-muted-button" disabled={page<=1} onClick={()=>load(page-1)}>قبل</button><span>{page} / {pages}</span><button type="button" className="admin-muted-button" disabled={page>=pages} onClick={()=>load(page+1)}>بعد</button></div><div className="admin-modal-actions"><button type="button" className="admin-primary-button" onClick={()=>setOpen(false)}>تأیید انتخاب‌ها</button></div></div></div>:null}</div>}
