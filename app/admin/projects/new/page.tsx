import Link from 'next/link';import ProjectForm from '../ProjectForm';import {createProject} from '../actions';import {getTenantContext} from '@/lib/platform/tenant-context'
export default async function NewProject(){const a=await getTenantContext();return <main className="admin-shell"><header className="admin-head"><div><span>PORTFOLIO CMS · {a.tenant.name}</span><h1>پروژه جدید</h1></div><Link className="admin-link" href="/admin/projects">بازگشت</Link></header><section className="admin-panel"><ProjectForm action={createProject} tenantId={a.tenantId}/></section></main>}
