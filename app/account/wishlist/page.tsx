import Link from'next/link';import{redirect}from'next/navigation';import{getPublicTenant}from'@/lib/platform/public-tenant';import{createClient}from'@/lib/supabase/server';import{toggleWishlistItem,setBackInStockAlert}from'../actions';export const dynamic='force-dynamic';
export default async function Wishlist(){const tenant=await getPublicTenant();if(!tenant)redirect('/');const s=await createClient(),{data:{user}}=await s.auth.getUser();if(!user)redirect('/account/auth');const[{data:items},{data:profile}]=await Promise.all([s.rpc('customer_wishlist_view',{p_tenant:tenant.id}),s.from('customer_profiles').select('locale').eq('tenant_id',tenant.id).eq('user_id',user.id).maybeSingle()]),en=profile?.locale==='en',list=Array.isArray(items)?items:[];return <main className="admin-shell" dir={en?'ltr':'rtl'}><header className="admin-head"><div><span>{en?'MY ACCOUNT':'حساب من'} · {tenant.name}</span><h1>{en?'Wishlist':'علاقه‌مندی‌ها'}</h1><p>{en?'Save products and subscribe to exact variants when they are out of stock.':'محصول‌ها و سایز/رنگ‌های مشخص را ذخیره کن و برای موجودشدن Variant دقیق اعلان بگیر.'}</p></div><Link className="admin-muted-button" href="/account">{en?'Back to account':'بازگشت به حساب'}</Link></header><section className="admin-panel">{!list.length?<p>{en?'Your wishlist is empty.':'هنوز چیزی به علاقه‌مندی‌ها اضافه نکرده‌ای.'}</p>:<div className="admin-log-list">{list.map((x:any)=>{const out=x.track_inventory&&Number(x.inventory_quantity||0)<=0;return <article className="admin-log-row" key={x.id}><div className="admin-log-row-head"><div><h3><Link href={`/shop/${x.product_slug}`}>{x.product_name||'Product'}{x.variant_title?` · ${x.variant_title}`:''}</Link></h3><small>{x.sku?`SKU: ${x.sku}`:''}</small></div>{x.variant_id?<strong>{Number(x.price||0).toLocaleString(en?'en-US':'fa-IR')} {x.currency}</strong>:null}</div>{x.variant_id?<p>{out?(en?'Out of stock':'ناموجود'):(en?'In stock':'موجود')}</p>:null}<div className="admin-actions"><form action={toggleWishlistItem}><input type="hidden" name="product_id" value={x.product_id}/><input type="hidden" name="variant_id" value={x.variant_id||''}/><button className="admin-muted-button">{en?'Remove':'حذف از علاقه‌مندی'}</button></form>{x.variant_id&&out?<form action={setBackInStockAlert}><input type="hidden" name="variant_id" value={x.variant_id}/><input type="hidden" name="active" value={x.alert_active?'false':'true'}/><button className="admin-primary-button">{x.alert_active?(en?'Disable stock alert':'خاموش کردن اعلان موجودی'):(en?'Notify me when available':'موجود شد خبرم کن')}</button></form>:null}</div></article>})}</div>}</section></main>}
