import{NextRequest,NextResponse}from'next/server';import{createClient}from'@/lib/supabase/server';function safeNext(v:string|null){return v&&v.startsWith('/')&&!v.startsWith('//')?v:'/account'}export async function GET(req:NextRequest){const url=new URL(req.url),code=url.searchParams.get('code'),next=safeNext(url.searchParams.get('next'));if(code){const s=await createClient();const{error}=await s.auth.exchangeCodeForSession(code);if(!error)return NextResponse.redirect(new URL(next,url.origin))}return NextResponse.redirect(new URL('/account/auth?error=confirmation_failed',url.origin))}
