'use client';import{useActionState}from'react';import{updateCustomerPassword,type CustomerAuthState}from'../actions';import'../customer-auth.css';const initial:CustomerAuthState={};export default function NewPassword(){const[state,action,pending]=useActionState(updateCustomerPassword,initial);return <main className="customer-auth" dir="rtl"><section className="customer-auth-card"><p className="eyebrow">امنیت حساب</p><h1>رمز عبور جدید</h1><p className="muted">یک رمز جدید و متفاوت از رمزهای قبلی انتخاب کن.</p><form action={action}><label>رمز جدید<input name="password" type="password" autoComplete="new-password" minLength={10} maxLength={128} required/></label><small>حداقل ۱۰ کاراکتر، شامل حرف و عدد.</small>{state.error&&<p className="auth-error">{state.error}</p>}<button disabled={pending}>{pending?'در حال ذخیره…':'ذخیره رمز جدید'}</button></form></section></main>}
