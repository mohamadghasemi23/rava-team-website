import'./customer-auth.css';import AuthForms from'./AuthForms';export const metadata={title:'حساب مشتری | Customer Account',robots:{index:false,follow:false}};function safeNext(v:unknown){const s=String(v||'').trim();return s.startsWith('/')&&!s.startsWith('//')&&s.length<=1200?s:'/account'}export default async function CustomerAuthPage({searchParams}:{searchParams:Promise<{next?:string}>}){const{next}=await searchParams;return <AuthForms next={safeNext(next)}/>}
