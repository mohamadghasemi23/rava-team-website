import type{Metadata}from 'next';import './globals.css';import AnalyticsTracker from './components/AnalyticsTracker';import TenantStatusGate from './components/TenantStatusGate';import {getPublicTenant}from '@/lib/platform/public-tenant'
export const metadata:Metadata={title:{default:'RAVA TEAM',template:'%s | RAVA TEAM'},description:'RAVA TEAM — Creative studio for strategy, branding and content production.',metadataBase:new URL(process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000'),openGraph:{title:'RAVA TEAM',description:'Creative studio for strategy, branding and content production.',type:'website'}}
export default async function RootLayout({children}:{children:React.ReactNode}){const tenant=await getPublicTenant();return <html lang={tenant?.default_locale?.startsWith('en')?'en':'fa'} dir={tenant?.default_locale?.startsWith('en')?'ltr':'rtl'}><body><AnalyticsTracker/>{tenant?<TenantStatusGate status={tenant.status} name={tenant.name}>{children}</TenantStatusGate>:children}</body></html>}
